{
  "name": "blast-response-tracker",
  "version": "1.0.0",
  "description": "Tracks GHL contacts through a blast and tags them based on whether they ever reply, checked on a delay/window rather than at send time.",
  "main": "src/index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/index.js",
    "migrate": "node src/migrate.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.12.0",
    "node-cron": "^3.0.3",
    "dotenv": "^16.4.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
